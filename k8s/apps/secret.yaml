# 서비스들이 공유하는 JWT 서명 시크릿(HS256) — 코어가 발급, 각 서비스가 로컬 검증.
# 데모용 리터럴이다. 운영은 SealedSecrets/ExternalSecrets(KMS/Vault)로 주입하고
# 이 파일 자체를 레포에 두지 않는다.
apiVersion: v1
kind: Secret
metadata:
  name: pay-jwt
type: Opaque
stringData:
  APP_JWT_SECRET: local-dev-only-please-override-with-32byte-min-secret-key
